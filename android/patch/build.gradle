/*
 * Fufcord Patch — Xposed/LSPatch-Modul: Fufcord direkt in Discord.
 * https://github.com/Fufi1925/Fufcord
 * Copyright (c) 2026 Fufcord. Alle Rechte vorbehalten.
 * Lizenziert unter der MIT-Lizenz (siehe LICENSE im Repo-Root).
 */

plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
}

android {
    namespace = "com.fufcord.patch"
    compileSdk = 34

    defaultConfig {
        applicationId = "com.fufcord.patch"
        minSdk = 26
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = "17"
    }
}

dependencies {
    // Nur zum Kompilieren — steckt zur Laufzeit in LSPosed/LSPatch.
    compileOnly("de.robv.android.xposed:api:82")
}
