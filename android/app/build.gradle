/*
 * Fufcord — https://github.com/Fufi1925/Fufcord
 * Copyright (c) 2026 Fufcord. Alle Rechte vorbehalten.
 * Lizenziert unter der MIT-Lizenz (siehe LICENSE im Repo-Root).
 */

plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
}

android {
    namespace = "com.fufcord.app"
    compileSdk = 34

    defaultConfig {
        applicationId = "com.fufcord.app"
        minSdk = 26
        targetSdk = 34
        versionCode = 16
        versionName = "1.13"
    }

    signingConfigs {
        stable {
            // Stabile Signierung ab v1.10: gleiche Signatur bei jedem Build → Updates
            // installieren sich direkt darüber (kein Deinstallieren mehr nötig).
            // Ohne Env-Variablen (lokal) → Standard-Debug-Signatur.
            def ksPath = System.getenv("FUF_KEYSTORE_PATH")
            if (ksPath) {
                storeFile file(ksPath)
                storeType "PKCS12"
                storePassword System.getenv("FUF_STORE_PASSWORD")
                keyAlias System.getenv("FUF_KEY_ALIAS")
                keyPassword System.getenv("FUF_KEY_PASSWORD")
            }
        }
    }

    buildTypes {
        debug {
            // Nur mit vorhandenem Schlüssel stabil signieren, sonst normal bauen.
            if (System.getenv("FUF_KEYSTORE_PATH")) {
                signingConfig signingConfigs.stable
            }
        }
        release {
            minifyEnabled = false
            proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro")
        }
    }
    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = "17"
    }
    buildFeatures {
        viewBinding = true
    }
}

dependencies {
    implementation("androidx.core:core-ktx:1.13.1")
    implementation("androidx.appcompat:appcompat:1.7.0")
    implementation("com.google.android.material:material:1.12.0")
    implementation("androidx.constraintlayout:constraintlayout:2.1.4")
    implementation("com.squareup.okhttp3:okhttp:4.12.0")
    implementation("androidx.security:security-crypto:1.0.0")
    implementation("androidx.viewpager2:viewpager2:1.0.0")
}
